package com.example.paymentprocessing.entity;

/**
 * TODO Kylian:
 * Define the user entity structure for account-related data.
 * Add fields, JPA mapping, and persistence-related annotations later.
 * Future field:
 * - paymentPasswordHash
 * Security rules:
 * - This field must store only the hashed payment password.
 * - Plain text payment passwords must never be stored in the database.
 * - Password hashing implementation will be added in a later stage.
 */
public class User {
    // TODO Define user entity fields and mappings, including future hashed payment password storage.
}

