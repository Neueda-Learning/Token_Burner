package com.example.paymentprocessing.dto.response;

/**
 * TODO Leon:
 * Define the response DTO for returning payment details.
 * Add response fields and mapping strategy later.
 *
 * Security rule:
 * - The response must never contain a payment password.
 * - The response must never contain a payment password hash.
 */
public class PaymentResponse {
    // TODO Add payment response fields while excluding password and password hash data.
}

